package agents

import (
	"testing"
)

func TestGenerateVideoSummary(t *testing.T) {
	t.Run("word limit test", func(t *testing.T) {
		// This is now covered in llm_test.go with more comprehensive tests
		// Keeping this file for future integration tests
	})
}